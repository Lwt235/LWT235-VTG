# Supervised Fine-Tuning (SFT) Configuration
# ===========================================

# Model configuration
model:
  # Base model from Hugging Face
  name_or_path: "Qwen/Qwen3-VL-4B-Instruct"
  # Alternative for faster testing: "Qwen/Qwen3-VL-2B-Instruct"
  
  # Trust remote code for model loading
  trust_remote_code: true
  
  # Precision settings
  torch_dtype: "bfloat16"
  attn_implementation: "flash_attention_2"
  
  # Token replacement for temporal outputs
  token_replacement:
    enabled: true
    # Map original tokens to temporal tokens
    replacements:
      "<|im_start|>": "<time_start>"
      "<|im_end|>": "<time_end>"

# LoRA configuration
lora:
  enabled: true
  r: 64
  lora_alpha: 128
  lora_dropout: 0.05
  # Target modules for LoRA adapters
  # When use_temporal_tokens=true, embed_tokens and lm_head are automatically added
  target_modules:
    - "q_proj"
    - "k_proj"
    - "v_proj"
    - "o_proj"
    - "gate_proj"
    - "up_proj"
    - "down_proj"
  bias: "none"
  task_type: "CAUSAL_LM"
  # Note: When temporal tokens are enabled, embed_tokens and lm_head
  # are automatically added to target_modules for proper adaptation

# Training configuration
training:
  # Output directory
  output_dir: "./outputs/sft"
  
  # Batch settings
  per_device_train_batch_size: 2
  per_device_eval_batch_size: 4
  gradient_accumulation_steps: 8
  
  # Training epochs/steps
  num_train_epochs: 3
  max_steps: -1  # -1 means use num_train_epochs
  
  # Learning rates (different parts of model)
  learning_rate: 1.0e-4
  mm_projector_lr: 1.0e-5  # Vision-language projector
  vision_tower_lr: 0.0     # Freeze vision encoder by default
  head_lr: 1.0e-4          # Task-specific head
  
  # Learning rate scheduler
  lr_scheduler_type: "cosine"
  warmup_ratio: 0.1
  warmup_steps: 0
  
  # Optimizer settings
  optim: "adamw_torch"
  adam_beta1: 0.9
  adam_beta2: 0.999
  adam_epsilon: 1.0e-8
  weight_decay: 0.01
  max_grad_norm: 1.0
  
  # Mixed precision
  bf16: true
  fp16: false
  
  # Gradient checkpointing
  gradient_checkpointing: true
  gradient_checkpointing_kwargs:
    use_reentrant: false
  
  # Logging
  logging_dir: "./outputs/sft/logs"
  logging_steps: 10
  logging_strategy: "steps"
  report_to: "tensorboard"  # or "wandb"
  
  # Evaluation
  eval_strategy: "steps"
  eval_steps: 500
  
  # Saving
  save_strategy: "steps"
  save_steps: 500
  save_total_limit: 3
  
  # Reproducibility
  seed: 42
  data_seed: 42
  
  # Other settings
  dataloader_num_workers: 4
  remove_unused_columns: false
  ddp_find_unused_parameters: false

# Loss configuration
loss:
  # Primary loss type: "cross_entropy", "smooth_l1", "combined"
  type: "cross_entropy"
  
  # For combined loss
  components:
    cross_entropy:
      weight: 1.0
    temporal_smoothness:
      weight: 0.1
      margin: 0.05
    boundary_regularization:
      weight: 0.05
  
  # Label smoothing
  label_smoothing: 0.0

# DeepSpeed configuration path (optional, override command line)
deepspeed: null
